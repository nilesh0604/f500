# Implementation Checklist — SCRUM-21

> Auto-generated by code-impl-agent. Each item maps to the TDD Spec Validation Checklist.
> ✅ = satisfied | ❌ = NOT satisfied (blocks code-test)

| #   | TDD Checklist Item                                                                       | Status | Notes                                                                                                                                                                                  |
| --- | ---------------------------------------------------------------------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | All acceptance criteria from requirements.md are covered                                 | ✅     | All 6 ACs implemented: valid query → async RAG answer; sig failure → 401; blank query → usage hint; immediate ack → async response_url; RAG error → ephemeral error; GET /health → 200 |
| 2   | API contract changes are backward-compatible (no breaking changes to existing consumers) | ✅     | New Lambda app; zero changes to vyasa-rag-service API                                                                                                                                  |
| 3   | New endpoints have auth middleware specified                                             | ✅     | receiver.ts verifies HMAC-SHA256 + 5-min timestamp window before any processing; worker has no public endpoint                                                                         |
| 4   | Error paths cover: invalid input, auth failure, downstream timeout                       | ✅     | 401 on bad sig; 200 ephemeral on empty query; 400 on invalid response_url; worker posts error on RAG failure/timeout; graceful degradation on response_url POST failure                |
| 5   | Sequence diagram matches the API contract (request/response shapes)                      | ✅     | Receiver returns `{ response_type, text }` JSON. Worker POSTs `{ response_type, replace_original, text }` to response_url. Matches TDD diagram.                                        |
| 6   | Rollback plan does not require manual DB surgery                                         | ✅     | No DB schema changes. Rollback = remove Slack slash command Request URL in Slack App admin.                                                                                            |
| 7   | Estimated complexity is realistic (M = 6 files, no DB migrations)                        | ✅     | 7 source files created (6 in app + 1 shared-type update); no migrations                                                                                                                |
| 8   | No requirements from requirements.md were silently dropped                               | ✅     | All 6 ACs and 6 edge cases from requirements.md are addressed                                                                                                                          |

## Files Created

- `apps/vyasa-slack-cmd/src/handlers/receiver.ts`: Lambda Function URL handler — HMAC verification, immediate ack, worker invocation
- `apps/vyasa-slack-cmd/src/handlers/worker.ts`: Async worker Lambda — RAG API call, response_url posting, error handling
- `apps/vyasa-slack-cmd/src/lib/slack-verifier.ts`: HMAC-SHA256 signature + 5-min timestamp window verification
- `apps/vyasa-slack-cmd/src/lib/response-formatter.ts`: RAG answer truncation (2900 chars) + citation label formatting
- `apps/vyasa-slack-cmd/src/lib/response-url-validator.ts`: SSRF guard — validates response_url against hooks.slack.com allowlist
- `apps/vyasa-slack-cmd/src/lib/logger.ts`: Winston structured logger (replaced stub `// test`)
- `apps/vyasa-slack-cmd/project.json`: Nx project config with multi-entry webpack build
- `apps/vyasa-slack-cmd/tsconfig.json`: TypeScript root config
- `apps/vyasa-slack-cmd/tsconfig.app.json`: TypeScript app config (extends tsconfig.base.json)
- `apps/vyasa-slack-cmd/webpack.config.js`: Multi-entry webpack producing handlers/receiver.js + handlers/worker.js

## Files Modified

- `libs/shared-types/src/index.ts`: Added `export * from './lib/slack.types'` (slack.types.ts already existed)
- `package.json`: Added `@aws-sdk/client-lambda ^3.1069.0` (installed; Lambda runtime provides it at deploy time)

## Build Result

`npx nx build vyasa-slack-cmd --skip-nx-cache` → **webpack compiled successfully**

Output:

- `dist/apps/vyasa-slack-cmd/handlers/receiver.js` → Lambda handler path: `handlers/receiver.handler`
- `dist/apps/vyasa-slack-cmd/handlers/worker.js` → Lambda handler path: `handlers/worker.handler`

## Manual Steps Required

1. **Install dependency**: `npm install` (already done; `@aws-sdk/client-lambda` now in node_modules)
2. **Create Secrets Manager secret**: `aws secretsmanager create-secret --name vyasa/slack/signing-secret --secret-string '{"signing_secret":"<YOUR_SLACK_SIGNING_SECRET>"}' --region us-east-1`
3. **Create worker Lambda** (`vyasa-slack-cmd-worker`): Node.js 22, 512 MB, 29s timeout, handler `handlers/worker.handler`; env vars: `SLACK_SECRET_ARN`, `VYASA_API_BASE_URL=https://lkbzhoe1pj.execute-api.us-east-1.amazonaws.com`, `RAG_TIMEOUT_MS=28000`
4. **Create receiver Lambda** (`vyasa-slack-cmd-receiver`): Node.js 22, 256 MB, 10s timeout, handler `handlers/receiver.handler`; env vars: `SLACK_SECRET_ARN`, `WORKER_LAMBDA_ARN=<worker ARN>`
5. **Enable Lambda Function URL** on receiver (Auth: NONE); copy the URL
6. **IAM — receiver**: grant `lambda:InvokeFunction` on worker ARN + `secretsmanager:GetSecretValue` on secret ARN
7. **IAM — worker**: grant `secretsmanager:GetSecretValue` on secret ARN  
   _(Note: worker doesn't need Secrets Manager at runtime since it doesn't re-verify the signature; the env var is present per TDD but unused in current worker implementation — can be removed)_
8. **Register Slack slash command** `/vyasa` in Slack App admin with Request URL = receiver Function URL
9. **Smoke test**: `/vyasa Who was Karna?` in a private Slack channel
